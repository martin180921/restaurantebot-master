# app_cliente — copia este archivo a .env y complétalo (nunca subas el .env)

# PostgreSQL del restaurante (Railway → pestaña Connect)
DATABASE_URL=postgresql://usuario:password@host:5432/railway

# Siempre 1 en el modelo deploy-por-restaurante
RESTAURANTE_ID=1
